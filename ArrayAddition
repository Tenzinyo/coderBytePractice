def ArrayAddition(arr):
    
    max_num = max(arr)
    arr.remove(max_num)
    sum = 0
    for i in range(len(arr)):
       sum+=arr[i]
    if sum == max_num:
        return True
    else:
        return False
        
print(ArrayAddition([4,6,10,23,3])) 
